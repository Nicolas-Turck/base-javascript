// create object in variable JavaScript

let episode = {
    title : 'dikkenek', 
    duration : 45, 
    hasBeenWatched : false
    };

// access to attributs in object 
let episodeTitle = episode.title;
let episodeDuration = episode.duration;
let episodeHasBeenWatched = episode.hasBeenWatched;